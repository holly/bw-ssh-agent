[project]
name = "bw-ssh-agent"
version = "1.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.15"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-mock>=3.15.1",
]
